const Loading = () => {
    return (
        <div className="min-h-screen border-2 flex items-center justify-center">
            <span className="loading loading-bars loading-lg"></span>
        </div>
    );
};

export default Loading;